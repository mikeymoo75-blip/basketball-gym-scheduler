DATABASE_URL="file:./dev.db"
AUTH_SECRET="replace-with-a-long-random-string"
AUTH_URL="http://127.0.0.1:43147"
EMAIL_FROM="MP Basketball <noreply@midlandpark.local>"
# Optional. Without this, welcome and cancellation emails are logged under Sent Emails.
RESEND_API_KEY=""

# Production (Proxmox + Cloudflare). On the VM, set AUTH_URL to the public site.
# AUTH_URL="https://www.datosfarm.com"
# CLOUDFLARE_TUNNEL_TOKEN=""
# AUTH_SECRET="paste-output-of-openssl-rand-base64-32"
# First-boot admin (only used when the database has no users yet)
# ADMIN_USERNAME=admin
# ADMIN_PASSWORD=MPtravel1!
